# C++ 虚函数与虚析构

## 虚函数

虚函数用于运行时多态。基类指针或引用调用虚函数时，会根据实际对象类型分派到派生类实现。

## 虚析构为什么重要

如果通过基类指针删除派生类对象，基类析构函数必须是虚函数，否则派生类析构函数可能不会被调用，导致资源泄漏或未定义行为。

## 面试回答

只要一个类打算被继承并通过基类指针管理生命周期，析构函数就应该声明为 virtual。协议状态机或消息处理框架中如果使用基类指针保存不同派生类型，这一点尤其重要。
