# sk_buff

## 定位

`sk_buff` 是 Linux 内核网络栈中表示网络包的核心结构。

## 它保存什么

- 报文数据指针
- 各层协议头位置
- 入接口和出接口信息
- 协议类型
- 路由和控制信息
- checksum、GSO、分片等元数据

## 面试回答

`sk_buff` 不只是报文内容本身，还包含内核处理这个报文所需的元数据。Linux 网络栈在不同层之间传递 `sk_buff`，通过调整头部指针和元数据完成协议解析、路由、过滤和发送。
